version: '3.8'

services:
  bowltrack:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - JWT_SECRET=change-this-to-a-random-string-in-production
    volumes:
      - ./data:/app/data
    restart: unless-stopped
